
<h1 class="pb-2 border-bottom">Liste des événements à venir</h1>
<p>Retrouve tous les grands sujets scientifiques animés par des passionnées</p>

<h3>Tous les événements! </h3>
    <div class="d-flex flex-wrap justify-content-center ">
        <% @events.each_with_index.map do |event, index| %>
        <%if event.is_future?%>
            <div class="card m-4 card-container" style="width:320px;">
                <div class="card-index card-header">
                    <h4><%=event.title%></h4>
                    <p>Organisateur: <%=event.admin.full_name%></p>
                </div>
                <div class="card-body align-content-between">
                    <div style="height:70px;text-overflow:fade(5px);"><p><%=event.description%></p>
                    <span id="points">...</span></div>
                </div>
                <div>
                        <ul class="list-group list-group-light">
                            <li class="list-group-item px-3">Date: <%=event.start_date%></li>
                            <li class="list-group-item px-3">Durée: <%=event.duration%> minutes</li>
                            <li class="list-group-item px-3">Prix: <%=event.price%> €</li>
                            <li class="list-group-item px-3">Lieu: <%=event.location%></li>
                        </ul></div>
                <div class="card-footer">
                <%=link_to "S'inscrire", event_path(event), :class => " btn btn-info" %>
                </div>
            </div>
        <%end%>
        <% end %>
    </div>


  <div class="container px-4 py-5" id="hanging-icons">
    <h2 class="pb-2 border-bottom">Hanging icons</h2>
    <div class="row g-4 py-5 row-cols-1 row-cols-lg-3">
      <div class="col d-flex align-items-start">
        <div class="icon-square bg-light text-dark flex-shrink-0 me-3">
          <svg class="bi" width="1em" height="1em"><use xlink:href="#toggles2"/></svg>
        </div>
        <div>
          <h2>Featured title</h2>
          <p>Paragraph of text beneath the heading to explain the heading. We'll add onto it with another sentence and probably just keep going until we run out of words.</p>
          <a href="#" class="btn btn-primary">
            Primary button
          </a>
        </div>
      </div>
      <div class="col d-flex align-items-start">
        <div class="icon-square bg-light text-dark flex-shrink-0 me-3">
          <svg class="bi" width="1em" height="1em"><use xlink:href="#cpu-fill"/></svg>
        </div>
        <div>
          <h2>Featured title</h2>
          <p>Paragraph of text beneath the heading to explain the heading. We'll add onto it with another sentence and probably just keep going until we run out of words.</p>
          <a href="#" class="btn btn-primary">
            Primary button
          </a>
        </div>
      </div>
      <div class="col d-flex align-items-start">
        <div class="icon-square bg-light text-dark flex-shrink-0 me-3">
          <svg class="bi" width="1em" height="1em"><use xlink:href="#tools"/></svg>
        </div>
        <div>
          <h2>Featured title</h2>
          <p>Paragraph of text beneath the heading to explain the heading. We'll add onto it with another sentence and probably just keep going until we run out of words.</p>
          <a href="#" class="btn btn-primary">
            Primary button
          </a>
        </div>
      </div>
    </div>
  </div>